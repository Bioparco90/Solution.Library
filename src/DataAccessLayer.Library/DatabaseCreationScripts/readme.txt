Per inizializzare il set di dati utilizzare per prima cosa lo script create-db.sql.
Un "Run to cursor" o opzione equivalente fa si che venga visualizzato fin da subito nell'explorer anche l'ultimo trigger definito,
in alternativa sarà necessario eseguire un refresh manuale.

Successivamente eseguire l'import manuale dei dati nelle tabelle avendo cura di eseguire per ultimo l'import delle reservations perchè
le Foreign Keys vi restituirebbero un errore